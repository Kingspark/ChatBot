{
  "name": "chatbot",
  "version": "1.0.0",
  "description": "ChatBot monorepo – backend entry for Hostinger Node.js deployment",
  "main": "backend/src/server.js",
  "scripts": {
    "build": "npm install && cd backend && npm install",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mysql2": "^3.15.3"
  },
  "engines": {
    "node": ">=18"
  }
}
